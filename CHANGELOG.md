# 更新日志

本项目遵循语义化版本。

## 1.1.2

- 将连接配置从 VS Code 工作区状态迁移到扩展全局状态，支持跨窗口共享
- 增加旧连接配置迁移过程的中英文进度和完成提示

## 1.1.1

- 调整文档、演示视频和操作手册
- 调整logo

## 1.1.0

- 连接 / 编辑连接改为多字段表单，新增「连接测试」按钮，保存前可校验连通性
- 断线重连优化：断连后由底层库在观察窗口内复用会话自动恢复，会话过期 / 认证失败 / 窗口超时才重建并停止后台重连
- 抽取共用节点数据编辑器（JSON / TXT 切换、换行、一键压缩），详情面板与新增节点表单复用
- 新增节点改为表单弹窗（父路径只读、类型下拉、数据编辑器复用）
- 节点图标：普通持久节点按是否叶子区分文件夹 / 文件图标，其余类型保留专属图标
- 导出子树命令文案调整为「导出节点及所有子节点数据...」（中英文同步）
- 优化节点详情布局：完整路径单行展示、stat 信息默认折叠并提供中英文可读说明，编辑/保存与显示选项分组排列
- 简化节点详情标签页标题：深层路径显示为 `.../父节点/当前节点`，页面内仍保留完整路径
- README 拆分为默认中文与独立英文手册，支持文档归档到 `docs/`
- 使用全新的 ZK 字形替换扩展商店与活动栏图标，并清理仓库开发环境文件

## 1.0.0

首次公开发布：

- 支持多连接、digest 认证、TLS 和断线重连
- 支持节点树懒加载、排序、搜索与路径定位
- 支持 JSON/TXT 查看和带版本校验的安全编辑
- 支持四种节点类型的创建、删除与递归删除
- 支持单节点及完整子树的无损导入导出
- 支持中文、English 和跟随 VS Code 的界面语言
