# 更新日志

本项目遵循语义化版本。

## Unreleased

- 连接 / 编辑连接改为多字段表单，新增「连接测试」按钮，保存前可校验连通性
- 断线重连优化：断连后由底层库在观察窗口内复用会话自动恢复，会话过期 / 认证失败 / 窗口超时才重建并停止后台重连
- 抽取共用节点数据编辑器（JSON / TXT 切换、换行、一键压缩），详情面板与新增节点表单复用
- 新增节点改为表单弹窗（父路径只读、类型下拉、数据编辑器复用）
- 节点图标：普通持久节点按是否叶子区分文件夹 / 文件图标，其余类型保留专属图标
- 导出子树命令文案调整为「导出节点及所有子节点数据...」（中英文同步）

## 0.1.0

首次公开发布：

- 支持多连接、digest 认证、TLS 和断线重连
- 支持节点树懒加载、排序、搜索与路径定位
- 支持 JSON/TXT 查看和带版本校验的安全编辑
- 支持四种节点类型的创建、删除与递归删除
- 支持单节点及完整子树的无损导入导出
- 支持中文、English 和跟随 VS Code 的界面语言
